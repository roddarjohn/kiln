class {{ schema_name }}(BaseModel):
    """{{ schema_name }} -- {{ slug }} representation."""

    type: Literal["{{ slug }}"] = "{{ slug }}"
{% for field in fields %}
    {{ field.name }}: {{ field.py_type }}
{% endfor %}

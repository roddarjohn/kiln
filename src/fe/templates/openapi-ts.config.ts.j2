// ============================================================
// AUTOGENERATED by `foundry generate --target fe`.
//
// The kiln-side source of truth is ``config/fe.jsonnet``.  Edit
// that file, then re-run ``just generate`` to regenerate this
// file and the openapi-ts output it drives.  Hand-edits here
// will be overwritten on the next ``just generate``.
// ============================================================

import { defineConfig } from "@hey-api/openapi-ts";

export default defineConfig({
  input: {{ openapi_spec | tojson }},
  output: {
    path: {{ output_dir | tojson }},
{% if format %}
    format: {{ format | tojson }},
{% endif %}
  },
  plugins: [
{% for plugin in plugins %}
    {{ plugin | tojson }},
{% endfor %}
  ],
});

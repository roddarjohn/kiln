// ============================================================
// AUTOGENERATED by fe_root from bootstrap.jsonnet.
//
// This file is a one-shot scaffold.  Edit it freely -- this is
// where your real app lives.  But running `just bootstrap`
// overwrites this file verbatim; fe_root has no merge
// semantics, so treat any post-bootstrap edits as "re-apply on
// next re-bootstrap".
// ============================================================

export default function App() {
  return (
    <main>
      <h1>{{ name }}</h1>
      <p>{{ description }}</p>
    </main>
  );
}

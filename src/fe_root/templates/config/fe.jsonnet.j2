// ============================================================
// AUTOGENERATED by fe_root from bootstrap.jsonnet.
//
// This file is a one-shot scaffold.  Edit it freely -- most
// apps tweak the OpenAPI spec path, the output directory, or
// the openapi-ts plugin set here.  But running `just bootstrap`
// overwrites this file verbatim; fe_root has no merge
// semantics, so treat any post-bootstrap edits as "re-apply on
// next re-bootstrap".
// ============================================================
//
// fe target configuration for {{ name }}.
//
// The fe target translates this file into
// ``openapi-ts.config.ts`` at the project root; @hey-api's
// openapi-ts then reads that config to produce the actual
// TypeScript client.  Run with::
//
//   just generate
//
// See https://heyapi.dev/ for the openapi-ts docs and
// https://roddarjohn.github.io/kiln/ for the fe schema.

{
  // Path or URL of the OpenAPI 3.x spec ``openapi-ts`` ingests.
  // Resolved relative to the project root (where ``yarn
  // openapi-ts`` runs from).
  openapi_spec: "{{ openapi_spec }}",

  // Where ``openapi-ts`` writes its generated client.  Lives
  // under ``src/`` by default so tsconfig and Vite pick it up
  // automatically.
  output_dir: "src/_generated",

  // Runtime client to wire up.  ``@hey-api/client-fetch`` uses
  // the platform's ``fetch`` and adds zero runtime deps; switch
  // to ``@hey-api/client-axios`` for axios-based projects.
  client: "@hey-api/client-fetch",

  // Generate React-Query hooks alongside the bare SDK.
  react_query: true,
}

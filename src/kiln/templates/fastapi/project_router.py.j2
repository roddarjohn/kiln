"""Root API router — mounts every app router.

Generated by kiln — re-run ``kiln generate`` to update.

Mount this in your application::

    from .routes import router
    app.include_router(router)
"""

from __future__ import annotations

from fastapi import APIRouter

{% for app in apps %}
from {{ app.module }}.routes import router as {{ app.module }}_router
{% endfor %}

router = APIRouter()
{% for app in apps %}
router.include_router({{ app.module }}_router, prefix="{{ app.prefix }}")
{% endfor %}

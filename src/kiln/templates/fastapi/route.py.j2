{% from "_macros.j2" import file_header %}
{{ file_header("FastAPI routes for " ~ model_name) }}

{{ import_block }}
router = APIRouter(prefix="{{ route_prefix }}", tags=["{{ model_name | lower }}"])

# ---------------------------------------------------------------------------
# Routes
# ---------------------------------------------------------------------------

{% for handler in route_handlers %}
{{ handler }}

{% endfor %}

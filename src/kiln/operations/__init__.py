"""New-protocol operations for kiln code generation.

Each module contains ``@operation``-decorated classes that
produce output objects in their ``build()`` method.  The engine
discovers and runs these; the assembler renders their output.
"""

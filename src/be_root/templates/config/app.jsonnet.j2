// ============================================================
// AUTOGENERATED by be_root from bootstrap.jsonnet.
//
// This file is a one-shot scaffold.  Edit it freely -- this is
// where you describe your app's resources to be.  But
// running `just bootstrap` overwrites this file verbatim;
// be_root has no merge semantics, so treat any
// post-bootstrap edits as "re-apply on next re-bootstrap".
// ============================================================
//
// App-level configuration for the ``{{ module }}`` package.
//
// Add resources to the ``resources`` list to expose them as
// CRUD endpoints.  Each resource is a small jsonnet object
// pointing at a SQLAlchemy model and listing the operations to
// generate; see the be docs for the full schema.

{
  version: "1",
  module: "{{ module }}",

  resources: [
    // Example -- delete or replace once you have real models::
    //
    //   {
    //     model: "{{ module }}.models.Widget",
    //     operations: [
    //       { name: "get" },
    //       { name: "list" },
    //       { name: "create" },
    //     ],
    //   },
{% if notification_preferences %}
    import "resources/notification_preference.jsonnet",
{% endif %}
  ],
}
